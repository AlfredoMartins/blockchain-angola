import { Header } from '@components/Header';
import { Container, Title } from './styles';

export function Groups() {
  return (
    <Container>
        <Title>Ango vote</Title>
    </Container>
  );
}